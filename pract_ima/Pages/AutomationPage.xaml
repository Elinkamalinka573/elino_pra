<Page x:Class="pract_ima.AutomationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Автоматизация"
      Background="#F4F7F9">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="⚙️ Правила автоматизации" 
                   FontSize="24" FontWeight="Bold"
                   Foreground="#1A5276"
                   HorizontalAlignment="Center"/>

        <Button Content="➕ Добавить правило" 
                Grid.Row="1"
                Width="150" Height="35"
                Margin="0,20"
                Background="#27AE60" 
                Foreground="White"
                Click="AddRule_Click"/>

        <ListView x:Name="RulesListView" Grid.Row="2">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" Width="200" DisplayMemberBinding="{Binding Название}"/>
                    <GridViewColumn Header="Условие" Width="300" DisplayMemberBinding="{Binding Условие}"/>
                    <GridViewColumn Header="Действие" Width="250" DisplayMemberBinding="{Binding Действие}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>